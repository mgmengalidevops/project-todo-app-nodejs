# Node.js Todo App with Server-Side Rendering

## Overview

This project is a Node.js todo application with server-side rendering, generated by AI to practice Docker containerization and deployment skills.

## Getting Started

### Running with Docker

Follow these steps to run the application in a Docker container:

```bash
# Create and run Ubuntu container
docker container run -p 8080:3000 --name app -it ubuntu /bin/bash

# Set up environment (inside container)
export DEBIAN_FRONTEND=noninteractive
apt update
apt-get install -y curl

# Install Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Verify installation
node -v

# Copy project files to container
docker container cp . app:/app

# Navigate to app directory and start
cd app
npm install
node app.js
```